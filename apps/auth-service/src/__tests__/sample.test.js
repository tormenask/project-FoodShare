// apps/auth-service/src/__tests__/sample.test.js
describe('Auth Service Initial Test', () => {
    test('should pass a basic sanity check', () => {
        // Verifica que 1 + 1 es igual a 2
        expect(1 + 1).toBe(2);
    });
});